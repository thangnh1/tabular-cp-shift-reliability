# Reliability of Tabular ML Under Distribution Shift — pinned requirements
# Python 3.12 (tested on Python 3.12.7, macOS 14, 16-core CPU)
#
# Versions match those used to produce the paper's results.

scikit-learn==1.5.1
numpy==1.26.4
pandas==2.3.3
scipy==1.13.1
xgboost==3.0.5
lightgbm==4.6.0
folktables==0.0.12
datasets<3.0
matplotlib>=3.8
pyarrow>=15
# Optional — only needed if you re-fetch raw ACS PUMS from an unblocked Census mirror
openml>=0.14
